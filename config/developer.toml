[developer]
# Keep robots enabled in normal operation. Developers can explicitly override
# this file for controlled internal testing, but the default UI never exposes it.
respect_robots = true
debug = false
max_sitemap_urls = 250
duplicate_similarity_threshold = 0.94
max_images_per_page = 4
minimum_text_length = 180

